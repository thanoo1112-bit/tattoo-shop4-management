'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function ArtistRootPage() {
  const router = useRouter();
  useEffect(() => {
    router.replace('/artist/dashboard');
  }, [router]);

  return (
    <div className="min-h-screen bg-studio-main flex items-center justify-center font-prompt">
      <span className="text-xs text-studio-secondary animate-pulse">??????????????????????????????????????????????????????????????? Artist Dashboard...</span>
    </div>
  );
}
